# Copy to terraform.tfvars (gitignored) and fill in.
account_id = "your-cloudflare-account-id"
domain     = "ptview.dev"

# Mirror of Linear workspace members.
allowed_emails = [
  "someone@example.com",
]
